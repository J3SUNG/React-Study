const path = require('path');

module.exports = {
   name: 'wordrelay-setting',
   mode: 'development',
   devtool: 'eval',

   entry: {

   }, 
   output: {
        path: path.join(__dirname, 'dist'),
   },
};